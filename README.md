# DelayPlugin

A basic delay plugin written in Rust using robbert-vdh's nih-plug framework. Controls include delay time, feedback and mix. 

## Building

After installing [Rust](https://rustup.rs/), you can compile DelayPlugin as follows:

```shell
cargo xtask bundle delay-plugin --release
```
